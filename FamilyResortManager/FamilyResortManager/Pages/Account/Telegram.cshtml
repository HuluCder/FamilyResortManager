@page
@model FamilyResortManager.Pages.Account.TelegramModel
@{
    ViewData["Title"] = "Telegram уведомления";
}

<div class="container py-4">
    <h2 class="mb-3">Настройка Telegram уведомлений</h2>
    @if (!string.IsNullOrEmpty(TempData["SuccessMessage"] as string))
    {
        <div class="alert alert-success">@TempData["SuccessMessage"]</div>
    }
    <p class="mb-2">
        Чтобы получать уведомления о новых задачах в Telegram:
    </p>
    <ol class="mb-4">
        <li>
            Откройте Telegram и перейдите к боту 👉 <a href="https://t.me/GetMyChatID_Bot" target="_blank">https://t.me/GetMyChatID_Bot</a>
        </li>
        <li>
            Нажмите <code>/start</code>, и бот пришлёт вам <strong>ваш Chat ID</strong>
        </li>
        <li>
            Скопируйте этот Chat ID (например: <code>123456789</code>)
        </li>
        <li>
            Вставьте его в поле ниже и нажмите <strong>Сохранить</strong>
        </li>
    </ol>
    <form method="post">
        <div class="form-group mb-3">
            <label>Ваш Telegram Chat ID:</label>
            <input type="text" name="ChatId" class="form-control"
                   value="@Model.CurrentChatId"
                   pattern="[0-9]{5,15}"
                   title="Введите корректный числовой chat_id (например: 123456789)"
                   required />
            <small class="form-text text-muted">
                Пример: <code>123456789</code>. Только цифры, без @@ и пробелов.
            </small>
        </div>
        <button type="submit" class="btn btn-primary">Сохранить</button>
    </form>
</div>